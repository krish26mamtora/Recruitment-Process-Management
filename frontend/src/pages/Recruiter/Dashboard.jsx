export default function Dashboard() {
  return (
    <div className="container">
      <section className="card">
        <h1>Recruiter Dashboard</h1>
        <p className="muted">Manage job postings and candidate pipelines.</p>
      </section>
    </div>
  )
}
